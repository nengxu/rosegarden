import glob
Import( "env KDEdoc" )
myenv=env.Copy()

## documentation to build
myenv.Meinproc( "index.docbook" )

myenv['APPNAME'] = 'rosegarden'
KDEdoc( 'sv', 'index.cache.bz2', myenv )
KDEdoc( 'sv', 'index.docbook', myenv )

for png in glob.glob("*.png"):
    KDEdoc( 'sv', png, myenv )

